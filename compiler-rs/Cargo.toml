[package]
name = "hydra"
version = "0.1.0"
edition = "2021"
description = "HydraScript compiler: Pythonic syntax for React (.hsx) and Node (.hs)"
license = "MIT"

[[bin]]
name = "hydra"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
