{
  "name": "hydrascript-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "runtime",
    "runtime-js",
    "vite-plugin",
    "demo2"
  ],
  "scripts": {
    "build:compiler": "cd compiler-rs && cargo build --release && cp target/release/hydra ../vite-plugin/bin/hydra && cp target/release/hydra ../editors/vscode/bin/hydra",
    "test": "npm run test:rust && npm run test:js",
    "test:rust": "cd compiler-rs && cargo test",
    "test:js": "node --test runtime-js/"
  }
}
